// Example utility to preprocess the image before OCR
function preprocessImage(imagePath) {
    // You could use libraries like sharp or jimp to preprocess the image
    // to improve OCR accuracy (resize, grayscale, etc.)
  }
  
  module.exports = {
    preprocessImage,
  };
  